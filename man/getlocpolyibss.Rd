% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign_positions.R
\name{getlocpolyibss}
\alias{getlocpolyibss}
\title{Assign IBSS positions using polygon lookup}
\usage{
getlocpolyibss(x, subloc = FALSE, survey = "")
}
\arguments{
\item{x}{A data frame or matrix whose \strong{first column is latitude} (decimal
degrees, south values may be supplied as positive or negative) and
\strong{second column is longitude} (decimal degrees east).}

\item{subloc}{Logical.  If \code{TRUE} the sub-location label is returned
(e.g. \code{"Fremantle Weed IBSS"}); if \code{FALSE} (default) the main site label
is returned (e.g. \code{"Fremantle IBSS"}).}

\item{survey}{Character string used to filter polygons by \code{SiteCode}.  An
empty string (default) matches all polygons.  Matching is
case-insensitive and uses \code{\link[base:grepl]{base::grepl()}}.}
}
\value{
A character vector of length \code{nrow(x)} giving the assigned IBSS
location for each input coordinate.  Points outside all polygons are
returned as \code{"notibss"}.
}
\description{
Tests whether GPS coordinates fall within any of the predefined IBSS survey
polygons and returns the corresponding site or sub-location label.  Points
that do not fall inside any polygon are labelled \code{"notibss"}.
}
\examples{
# Single point inside the Fremantle Weed polygon
coords <- data.frame(Lat = -32.18, Lon = 115.48)
getlocpolyibss(coords)

# Return sub-location labels, filtered to Fremantle polygons only
getlocpolyibss(coords, subloc = TRUE, survey = "Fremantle")
}
\seealso{
\code{\link[=getlocspointibss]{getlocspointibss()}} for nearest-centroid assignment,
\code{\link[=getlocpolyiss]{getlocpolyiss()}} for the ISS equivalent, \link{ibsspoly} for the underlying
polygon data.
}
