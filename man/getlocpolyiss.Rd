% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign_positions.R
\name{getlocpolyiss}
\alias{getlocpolyiss}
\title{Assign ISS positions using polygon lookup}
\usage{
getlocpolyiss(x, subloc = FALSE, survey = "")
}
\arguments{
\item{x}{A data frame or matrix whose \strong{first column is latitude} (decimal
degrees, south values may be supplied as positive or negative) and
\strong{second column is longitude} (decimal degrees east).}

\item{subloc}{Logical.  If \code{TRUE} the sub-location label is returned
(e.g. \code{"Fremantle Weed IBSS"}); if \code{FALSE} (default) the main site label
is returned (e.g. \code{"Fremantle IBSS"}).}

\item{survey}{Character string used to filter polygons by \code{SiteCode}.  An
empty string (default) matches all polygons.}
}
\value{
A character vector of length \code{nrow(x)} giving the assigned ISS
location.  Points outside all polygons are returned as \code{"notiss"}.
}
\description{
Tests whether GPS coordinates fall within any of the predefined ISS survey
polygons and returns the corresponding site or sub-location label.  Points
that do not fall inside any polygon are labelled \code{"notiss"}.
}
\examples{
coords <- data.frame(Lat = -30.5, Lon = 114.9)
getlocpolyiss(coords)

getlocpolyiss(coords, subloc = TRUE, survey = "Jurien")
}
\seealso{
\code{\link[=getlocspointiss]{getlocspointiss()}} for nearest-centroid assignment,
\code{\link[=getlocpolyibss]{getlocpolyibss()}} for the IBSS equivalent, \link{isspoly} for the underlying
polygon data.
}
