% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign_positions.R
\name{getlocspointibss}
\alias{getlocspointibss}
\title{Assign IBSS positions using nearest-centroid lookup}
\usage{
getlocspointibss(x, subloc = FALSE, survey = "IBSS")
}
\arguments{
\item{x}{A data frame or matrix whose \strong{first column is latitude} (decimal
degrees, south values may be supplied as positive or negative) and
\strong{second column is longitude} (decimal degrees east).}

\item{subloc}{Logical.  If \code{TRUE} the sub-location label is returned
(e.g. \code{"Fremantle Weed IBSS"}); if \code{FALSE} (default) the main site label
is returned (e.g. \code{"Fremantle IBSS"}).}

\item{survey}{Character string used to filter the candidate polygon set by
\code{Site} name (default \code{"IBSS"}).  Set to \code{""} to consider all polygons.}
}
\value{
A character vector of length \code{nrow(x)} giving the closest IBSS
location for each input coordinate.
}
\description{
For each input coordinate, computes the Euclidean distance to the centroid
of every IBSS polygon and returns the label of the closest polygon.  Unlike
\code{\link[=getlocpolyibss]{getlocpolyibss()}}, this function always assigns a location — useful for
points that are just outside a polygon boundary due to GPS imprecision.
}
\details{
Distances are computed in degrees (not metres) which is adequate for the
spatial scales of the IBSS sites.
}
\examples{
coords <- data.frame(Lat = -32.18, Lon = 115.48)
getlocspointibss(coords, subloc = TRUE)
}
\seealso{
\code{\link[=getlocpolyibss]{getlocpolyibss()}} for the strict polygon-based equivalent,
\code{\link[=getlocspointiss]{getlocspointiss()}} for the ISS version, \link{ibsspoly} for the polygon data.
}
